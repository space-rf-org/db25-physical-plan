#pragma once
// db25-physical-plan version marker. Real public headers (the physical IR, the
// Cascades memo, the planner entry point) arrive with the Increment 0 units.
namespace db25::physical {
inline constexpr int kVersionMajor = 0;
inline constexpr int kVersionMinor = 1;
inline constexpr int kVersionPatch = 0;
}  // namespace db25::physical
